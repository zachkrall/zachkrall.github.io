"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _chalk = require("chalk");

var _chalk2 = _interopRequireDefault(_chalk);

var _logSymbols = require("log-symbols");

var _logSymbols2 = _interopRequireDefault(_logSymbols);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var INFO = "info";
var SUCCESS = "success";
var WARN = "warning";
var ERROR = "error";

// from log-symbols


var log = function log(sender, symbol, message, writer) {
  var msg = (symbol ? _logSymbols2.default[symbol] : " ") + " " + _chalk2.default.gray(sender + ": ") + " " + message;
  if (writer) {
    writer(msg);
  }
  return msg;
};

exports.default = function (sender) {
  var writer = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : console.log;

  var logger = function logger(msg) {
    return log(sender, INFO, msg, writer);
  };
  logger.debug = function (msg) {
    return log(sender, null, msg, writer);
  };
  logger.info = function (msg) {
    return log(sender, INFO, msg, writer);
  };
  logger.success = function (msg) {
    return log(sender, SUCCESS, msg, writer);
  };
  logger.warn = function (msg) {
    return log(sender, WARN, msg, writer);
  };
  logger.error = function (msg) {
    return log(sender, ERROR, msg, writer);
  };
  return logger;
};